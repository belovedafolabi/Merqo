# Merqo — Agent Instructions

Merqo: a configurable, multi-business-type Point-of-Sale platform, deployed
independently per client. See `docs/milestones/README.md` for the implementation
roadmap and `docs/milestones/DECISIONS_AND_CONFLICTS.md` for the authoritative
resolution of conflicting decisions across the design corpus in `docs/`.

## Agent skills

### Issue tracker

Issues live as GitHub issues in `belovedafolabi/Merqo`, managed via the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Triage labels

The five canonical triage roles, used verbatim as label strings. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context: `CONTEXT.md` and `docs/adr/` at the repo root. See `docs/agents/domain.md`.
